import AuthFormContainer from 'components/custom/AuthFormContainer/AuthFormContainer';

const ResetPasspage = () => {
  return <AuthFormContainer authType={'sent'} />;
};

export default ResetPasspage;
